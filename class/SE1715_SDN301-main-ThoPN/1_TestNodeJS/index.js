import {Add, Sub, Mul} from './calc.js';

// Gọi hàm để thực thi
console.log(`Sum x + y = ${Add(10,20)}`);
console.log(`Substraction x - y = ${Sub(10,20)}`);
console.log(`Multiply x * y = ${Mul(10,20)}`);